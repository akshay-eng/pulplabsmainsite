node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vite

# Vendored Claude Code skills (three.js, 3d-web-experience, ui-ux-pro-max).
# Local tooling, not part of the site: third-party code, and its SKILL.md
# carries absolute machine paths. Reinstall from the upstream repos if needed.
.claude/

# SQLite database + local env
.data/
*.db
*.db-wal
*.db-shm

# Next.js
.next/
next-env.d.ts

# Agent skills installed by `npx skills add` — vendored third-party
# instruction files, not this site's source.
.agents/
skills-lock.json

# Kubernetes secrets: real credentials, never committed.
k8s-deployment/secret.yaml
